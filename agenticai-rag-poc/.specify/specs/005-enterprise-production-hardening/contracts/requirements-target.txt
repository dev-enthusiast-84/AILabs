# Target requirements after LangGraph/LangChain ecosystem upgrade.
# This file defines the intended version pins — it is NOT the live requirements.txt.
# Use it as a reference contract when implementing the upgrade tasks.

# ── LangChain ecosystem ───────────────────────────────────────────────────────
# All packages must be upgraded together — langchain-core 1.x is the shared
# compatibility boundary that pulls the entire set forward.
langchain==1.3.1
langchain-core==1.4.0
langchain-openai==1.2.1
langchain-community==0.4.1
langchain-text-splitters==1.1.2    # NEW: split out from langchain package

# ── LangGraph ─────────────────────────────────────────────────────────────────
# 1.x fixes the Reviver/allowed_objects deprecation warning and moves to
# actively maintained codebase. Breaking change: set_entry_point removed.
langgraph==1.2.0                   # was 0.2.73
# langgraph-checkpoint and langgraph-prebuilt are resolved as transitive deps:
#   langgraph-checkpoint==4.1.0    (was 2.1.2)
#   langgraph-prebuilt==1.1.0      (new, split from langgraph)
#   langgraph-sdk==0.3.14          (was 0.1.74)

# ── Optional (dev / local only) ───────────────────────────────────────────────
langchain-chroma==1.1.0            # was 0.2.2; requires chromadb>=1.0.0
langchain-experimental==0.4.1     # was 0.3.4; SemanticChunker API unchanged

# ── Forced transitive upgrades ────────────────────────────────────────────────
# These are NOT directly referenced in app code. They are forced by the above.
# openai==2.37.0        (was 1.61.1 — forced by langchain-openai 1.2.1)
# chromadb==1.5.9       (was 0.6.3  — forced by langchain-chroma 1.1.0)
# pydantic-settings==2.14.1 (patch update — backward compatible)
